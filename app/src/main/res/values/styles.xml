<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

        <item name="primaryTextColor">@android:color/primary_text_dark_nodisable</item>
        <item name="secondaryTextColor">@android:color/black</item>
        <item name="authorTextColor">@android:color/secondary_text_light</item>
        <item name="backgroundColor">@android:color/white</item>
        <item name="dialogBackground">@android:drawable/dialog_holo_light_frame</item>
    </style>

    <!-- Dark mode application theme-->
    <style name="AppThemeDark" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

        <item name="primaryTextColor">@android:color/primary_text_dark_nodisable</item>
        <item name="secondaryTextColor">@android:color/white</item>
        <item name="authorTextColor">@android:color/secondary_text_dark</item>
        <item name="backgroundColor">@android:drawable/dark_header</item>
        <item name="dialogBackground">@android:drawable/dialog_holo_dark_frame</item>
    </style>

    <!--Shadow Preset-->
    <style name="ShadowedText">
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PreferenceTheme" />


</resources>
